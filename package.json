{
  "name": "spec-harness",
  "version": "0.1.0",
  "description": "Spec-driven agent harness: 7 roles, 5 code-enforced gates (Gate 5 is an adversarial agent), AC traceability from spec to test evidence.",
  "bin": { "spec-harness": "install.mjs" },
  "type": "module",
  "engines": { "node": ">=20" },
  "files": ["install.mjs", "kernel/", "skills/", "agents/", "adapters/", "commands/", "hooks/"],
  "scripts": { "test": "node install.mjs --self-test" },
  "license": "MIT"
}
