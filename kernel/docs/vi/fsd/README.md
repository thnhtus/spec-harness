# fsd/ — artifact tham chiếu của project (agent đọc, KHÔNG sửa)

> Bản dịch của [`../../fsd/README.md`](../../fsd/README.md). Lệch nhau → bản tiếng Anh thắng.

Thư mục này **rỗng khi vừa cài**. Project tự điền vào, hoặc xoá thư mục nếu
không dùng — kernel chỉ trỏ tới đây, nó không bắt buộc phải có file nào.

Giữ mục lục ngay trong file này: agent bị cấm đọc cả một thư mục
(SharedRules §8); chúng tra bảng dưới đây và mở **đúng một file**.

| ID / file | Nội dung |
| --- | --- |
|  |  |
